---
import type { ImageMetadata } from "astro";
import { getCollection } from "astro:content";
import { Image } from "astro:assets";

const news = await getCollection("news");


---

<div
  class="mb-12 flex max-w-72 flex-col justify-between rounded-md border border-neutral-700 bg-yellow-100">
  {
  <h2 class="px-3 pt-3 text-2xl font-semibold">{pubDate}</h2>
  <p>{text}</p>
}
</div>
